extends layout

block content
  .landing-page
    .landing-center
      .logo-hero
        svg(viewBox="0 0 60 60" xmlns="http://www.w3.org/2000/svg")
          circle(cx="30" cy="30" r="30" fill="red")
          polygon(points="24,18 24,42 46,30" fill="white")
        span.logo-text Videx
      a.join-btn(href="/videos") JOIN